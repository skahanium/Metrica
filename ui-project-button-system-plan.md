# Metrica 项目按钮体系与左侧边栏重规划

## Summary

当前界面不应继续保留一排语义含混的顶部按钮，也不应让左侧边栏重复承担导入数据、查看全部数据等主动作。新的界面体系采用：

- 顶部只负责项目级、数据级、导出级入口。
- 主面板负责命令与结果消息流。
- 左侧边栏改造成类似 Stata 右侧栏的上下两区，但位置仍保持在项目现有的左侧。
- `.metrica` 是用户可见的单文件项目，CSV 是外部数据来源，不和命令、结果混在同一个数据文件里。

## 项目文件语义

Metrica 项目文件采用单个 `.metrica` 文件。用户不应看到 `.json` 文件、隐藏目录或内部持久化细节。

`.metrica` 内部建议使用 SQLite 单文件保存项目状态。该文件保存：

- 项目元信息。
- CSV 相对路径引用。
- 保存范式：仅命令，或命令加结果。
- 命令消息。
- 可选结果快照。
- 消息删除状态与回收站。
- 数据历史。
- 变量元数据。
- 当前 UI 状态。

原始 CSV 不打包进 `.metrica`。项目默认保存 CSV 的相对路径，方便把 `.metrica` 和 CSV 所在研究文件夹一起移动或分享。

保存范式由用户在首次保存或另存为时选择：

- 仅命令：重新打开后只看到命令消息，结果需要重跑。
- 命令加结果：重新打开后恢复命令和结果消息流。

普通保存沿用当前项目的保存范式；若要切换范式，使用另存为。

## 顶部按钮体系

顶部主按钮收敛为三个入口：

1. 项目
2. 数据
3. 导出

删除现有顶部的：

- 新建项目
- 打开项目
- 保存项目
- 重跑上次
- 教学开 / 教学关

这些能力不是消失，而是归入更清晰的入口。

### 项目

`项目` 菜单包含：

- 新建 `.metrica` 项目。
- 打开 `.metrica` 项目。
- 保存当前项目。
- 另存为 `.metrica` 项目。
- 选择或查看当前保存范式。
- 最近项目。
- 关闭项目。

项目动作只处理 `.metrica` 项目文件，不负责导入 CSV，不负责导出结果文件。

### 数据

`数据` 菜单包含：

- 导入 CSV。
- 查看全部数据。
- 数据变换。
- 数据历史。

数据历史用于恢复活动数据集到任意历史状态。它不是消息删除，也不是运行记录删除。

### 导出

`导出` 菜单包含：

- 导出命令脚本。
- 导出当前结果。
- 导出选中结果。
- 导出项目报告。
- 导出当前活动数据 CSV。

桌面端导出必须走原生保存文件对话框，不再依赖浏览器式 `<a download>`。

### 重跑

不再提供全局 `重跑上次`。

重跑入口放在具体命令消息或结果消息上：

- 重跑此命令。
- 重跑选中命令。
- 重跑当前可复现命令序列。

重跑使用结构化命令请求，不以展示文本作为执行依据。

### 教学解释

教学解释不再作为顶部主按钮。

它应成为结果消息内部的显示开关，仅控制该结果的教学解释层是否展示。

## 左侧边栏体系

项目继续使用单一左侧边栏，不新增右侧边栏。

左侧边栏借鉴 Stata 右侧栏的结构，分为上下两部分：

- 上半部分：变量窗口。
- 下半部分：信息窗口。

左侧边栏不再放：

- 数据源区块。
- 选择 CSV 文件按钮。
- 查看全部数据按钮。

这些动作统一进入顶部 `数据` 菜单。

## 变量窗口

变量窗口显示变量名和变量标签。

变量名是当前活动数据集里的当前列名。变量标签默认等于该列首次导入时的原始变量名。

行为规则：

- 单击变量名：把变量名插入 CLI 当前光标位置。
- 双击变量名：触发变量重命名。
- 重命名必须走和 CLI 一致的数据操作链。
- UI 重命名等价于 CLI `rename old new`。
- 重命名成功后，主面板出现与 CLI rename 等价的数据变换结果消息。
- 多次重命名后，标签仍保持首次导入时的原始变量名不变。

示例：

```text
首次导入列名：x1
第一次重命名：x1 -> income
第二次重命名：income -> household_income

当前变量名：household_income
变量标签：x1
```

变量标签在本阶段不提供用户编辑，只用于追溯来源。

## 信息窗口

信息窗口显示两类信息：

- 数据集信息。
- 当前选中变量信息。

数据集信息包括：

- 数据来源。
- 数据标签。
- 变量数。
- 观测数。
- 数据大小。
- 当前活动数据状态。

选中变量信息包括：

- 当前名称。
- 标签。
- 数据类型。
- 格式。
- 缺失数。
- 唯一值数。

未选中变量时，只显示数据集信息。

如果当前数据是派生数据，应明确区分：

- 原始 CSV 来源。
- 当前活动数据状态。

无法从当前协议得到的信息显示为“未记录”，不得伪造。

## 消息流与回收站

主面板中的命令和结果采用消息流语义，类似聊天记录。

支持：

- 单选。
- 多选。
- 全选。
- 删除。
- 从回收站恢复。
- 彻底删除。

删除消息只影响展示，不改变数据状态。

例如，用户运行了自动插补命令后，当前活动数据已经变成插补后的数据。删除这条插补结果消息，只是隐藏该消息和对应展示结果，不会撤销插补。

如果用户要回到插补前的数据，必须进入：

```text
数据 -> 数据历史
```

并恢复到插补前的数据状态。

## 数据历史

数据历史记录每一次改变活动数据集的操作，例如：

- filter
- generate
- replace
- rename
- drop
- keep
- sort
- merge
- reshape
- collapse
- impute_missing

每个历史节点保存：

- 数据状态 id。
- 来源状态 id。
- 操作类型。
- 操作参数。
- 活动数据路径。
- 行列变化。
- notes / warnings。
- 创建时间。

用户可以恢复到任意历史点。

恢复历史点会改变当前活动数据集、变量窗口和信息窗口，但不会删除消息流里的命令或结果消息。

## CLI / UI 等价

所有鼠标操作都应尽量落到同一套 CLI / DataOp / Runtime 执行链。

特别是：

- UI 双击变量重命名等价于 CLI `rename old new`。
- 顶部数据变换等价于 CLI 数据操作命令。
- 主面板结果消息应显示对应命令或等价命令标签。
- 重跑使用结构化请求，不依赖展示文本重新解析。

## Public Interface / Type Changes

新增或约定变量元数据字段：

```ts
interface VariableMetadata {
  current_name: string;
  original_name: string;
  label: string;
  dtype?: string;
  format?: string;
  missing_count?: number;
  unique_count?: number;
  source_state_id?: string;
}
```

UI 重命名生成统一数据操作：

```ts
{
  op: 'rename',
  args: {
    mapping: {
      [oldName]: newName
    }
  }
}
```

`.metrica` SQLite 项目文件至少需要支持以下逻辑表：

- project_meta
- data_sources
- data_states
- variable_metadata
- messages
- commands
- results
- trash
- ui_state

原生壳需要新增：

- 选择 `.metrica` 项目文件。
- 保存 `.metrica` 项目文件。
- 选择 CSV 文件。
- 保存导出文件。

## Test Plan

### 顶部按钮

- 顶部只显示 `项目`、`数据`、`导出`。
- 不再显示 `新建项目`、`打开项目`、`保存项目`、`重跑上次`、`教学开/关`。
- `数据` 菜单包含导入、查看全部数据、数据变换、数据历史。
- `导出` 菜单根据当前选择和项目状态启用或禁用对应导出项。

### 左侧边栏

- 左侧边栏不再显示原 `数据源` 区块。
- 左侧边栏不再显示 `选择 CSV 文件`。
- 变量窗口显示当前变量名和固定标签。
- 单击变量名插入 CLI 当前光标位置。
- 双击变量名触发 rename 数据操作。
- 多次重命名后标签仍保持首次导入名。

### 信息窗口

- 加载 CSV 后显示来源、变量数、观测数、数据大小。
- 选中变量后显示名称、标签、类型、格式、缺失数、唯一值数。
- 派生数据激活后显示当前活动数据状态。
- 字段缺失时显示“未记录”，不显示虚假值。

### 消息流

- 命令和结果可单选、多选、全选。
- 删除消息进入回收站。
- 回收站恢复后消息重新出现在消息流。
- 彻底删除只删除消息记录，不误删 CSV 或派生数据。
- 删除数据变换消息不撤销活动数据状态。

### 数据历史

- 每次数据变换生成一个数据历史节点。
- 用户可恢复到任意历史点。
- 恢复历史点会刷新活动数据、变量窗口和信息窗口。
- 恢复历史点不删除消息流。

### 项目文件

- 新建 `.metrica` 创建单文件项目。
- 打开 `.metrica` 恢复项目状态。
- 仅命令模式重开后只恢复命令。
- 命令加结果模式重开后恢复命令和结果。
- CSV 路径以相对路径优先保存。

## Assumptions

- 项目只使用现有左侧边栏，不新增右侧边栏。
- 左侧边栏采用 Stata 右侧栏的职责和上下结构，但位置保持在左侧。
- 原始 CSV 不嵌入 `.metrica`。
- `.metrica` 是 SQLite 单文件，用户不接触内部结构。
- 变量标签在本阶段固定为首次导入时的原始变量名。
- 消息删除和数据历史恢复是两套不同机制。
