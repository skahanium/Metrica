# Task 1 — MetricaBase structured contract tests
# Plan: docs/superpowers/plans/2026-04-24-metrica-alpha-vertical-slice-plan.md
#
# When you add @test / @testset, add a `test/Project.toml` with deps + [sources],
# or run tests from the repo root env (`julia --project=D:\\Metrica`) so `Test` resolves.
using MetricaBase

@assert true  # placeholder; replace with Test.jl tests
