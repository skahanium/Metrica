# S5 高级计量模型族全量成熟化施工方案

**目标:** 将 S5 从“高级专题基础实现”升级为完整、可信、可教学、可展示、可扩展的高级计量模型族能力层，并以世界一流开源计量项目标准补齐模型、估计方法、诊断指标、效应分解、预测、结构化输出、测试与文档。

## Summary

本方案不再以“最小闭环”作为施工目标。S5 的完成标准应从“某个模型能跑通”提升为“模型族具备成熟能力”：同一模型族内的主流模型、估计器、诊断、效应、预测、边界警告、结构化结果和桌面展示必须协同完成。

施工可以分阶段提交，但每个阶段都必须服务于最终完整能力，不得再以 `partial` 当作终态。允许短期存在 `planned` 状态，但必须在计划内给出明确落地任务、接口和测试标准。

## 当前实现差距基线

截至本方案更新时，仓库已具备部分 S5 垂直能力，但距离“模型族成熟化”仍有显著差距。下面表格是后续施工的真实账本，不得用单个模型可运行替代整族完成。

| 模型族 | 当前已实现或已接入 | 主要缺口 | 成熟化完成度估计 |
|---|---|---|---|
| 统一能力协议 | `ModelCapabilities` 已在 `MetricaBase.jl` 定义，空间模型初步返回 `model_capabilities` | 其他 S5 包尚未统一接入；`augment_status`、`effects`、`predictions` 仍未形成跨包标准 | 20% |
| 空间计量 | `spatial_lag`、`spatial_error`、`spatial_slx`；SAR/SLX 效应分解；Moran I 的方差、z 值、p 值；App 空间诊断展示 | 缺 SDM、SDEM、SAC/SARAR、空间 Probit、GWR、GTWR、LM/Robust LM 检验、kNN/distance-band 权重构造、shapefile/GeoPackage 输入 | 25% |
| 动态面板 GMM | Difference GMM 基础能力 | 缺 System GMM、collapsed instruments、Difference-in-Hansen、工具变量膨胀治理、跨工具 golden tests | 30% |
| ARCH/GARCH | ARCH/GARCH 常数均值基础估计 | 缺参数标准误、t/skew-t、ARMA/ARX 均值、GJR/EGARCH、forecast、VaR/ES、回测 | 25% |
| Duration / Survival | Cox PH 基础、HR 与基础删失摘要 | 缺 Schoenfeld/PH 检验、strata、cluster robust SE、time-varying covariates、AFT 参数模型 | 25% |
| GMM | 线性 IV-GMM 基础 | 缺通用 moment、iterated GMM、CUE、C-stat、Difference-in-Hansen、弱工具变量诊断 | 30% |
| System | SUR、system 2SLS、system 3SLS 基础 | 缺跨方程约束检验、系统级 Wald/LR/LM、robust/cluster covariance、系统预测 | 35% |
| Quantile | 单 τ 分位数回归基础 | 缺多 τ、bootstrap/rank/sparsity inference、quantile process、IV quantile、panel quantile | 25% |
| Nonlinear / Threshold | 受控 NLS 与单门限基础 | 缺 NLS 协方差/CI、多起点诊断、用户公式扩展、threshold bootstrap CI、sup-Wald、多阈值 | 20% |
| Bayesian | 仅规划口径，当前不可调用 | 缺 `MetricaBayes.jl`、Core/Runtime/App 全链路、共轭贝叶斯线性回归、后验预测、MCMC 诊断 | 0% |
| App 展示 | 若干模型族已有专属面板，空间面板开始展示效应 | 动态面板、GARCH、Survival、Quantile、Bayes 的成熟展示仍不足 | 25% |
| 数值验证 | 部分包有确定性测试，空间新增基础行为测试 | 大多数 S5 模型缺跨工具 golden value、边界语义、展示状态测试 | 20% |

整体判断：当前 S5 离本方案定义的“全量成熟化”仍很远，整体完成度约为 20%–30%。后续不得把已完成的 SAR/SEM/SLX 或单一模型测试误称为 S5 完成。

## Key Changes

- 新增统一模型能力协议：每个 S5 结果必须输出 `model_capabilities`，包含 `status`、`model_family`、`supported_models`、`estimators`、`diagnostics_available`、`diagnostics_unavailable`、`effects_available`、`prediction_available`、`limitations`。
- 新增统一结构化警告协议：所有数值限制、样本变化、不可逆矩阵、未收敛、近奇异、诊断不可计算都必须返回机器可读 warning code。
- Runtime 只校验与转发能力协议，不实现计量逻辑；App 只展示结构化结果与解释，不自行推断模型含义。
- 文档、Runtime schema、App 类型、Julia Core 支持模型必须一致；禁止文档宣称可用但代码不可用。
- 测试标准从 shape test 升级为 golden value、边界语义、跨层协议、桌面展示四类测试。

## Implementation Changes

### 1. 协议与跨层一致性

修改 `MetricaBase.jl` 或现有共享协议层，定义 S5 通用能力元数据和 warning schema。所有 S5 包的结果对象统一包含 `glance`、`tidy`、`augment_status`、`diagnostics`、`effects`、`predictions`、`warnings`、`model_capabilities`。

同步更新 `docs/architecture/runtime-protocol.md`、Runtime model whitelist、App `ModelSpec` 类型和结果渲染类型。`bayes_linear` 等尚未实现能力只能出现在“计划能力”表，不得出现在当前可调用模型枚举里，除非同时补齐 Core、Runtime、App、测试。

### 2. 空间计量模型族全量补强

`MetricaSpatial.jl` 从 SAR/SEM 基础实现扩展为完整空间横截面模型族。必须实现并测试：

- 空间滞后类：SAR、SDM、SAC/SARAR。
- 空间外生滞后类：SLX、SDEM。
- 空间误差类：SEM、SAC/SARAR。
- 离散选择空间模型：空间 Probit 的可用实现，若算法复杂可先限制小样本 dense 权重矩阵，但必须真实可估计。
- 局部加权空间/时空模型：GWR、GTWR，支持 fixed/adaptive bandwidth、Gaussian/Bisquare kernel、CV/AICc 带宽选择、局部系数、局部标准误、局部 t 值、局部 R²、有效参数数、AICc 与局部诊断。这里的 GWR 即 Geographically Weighted Regression，和“地理加权模型 GWR”是同一个模型，不是另一个同名能力。
- 空间诊断：Moran’s I、LM lag、LM error、Robust LM lag/error、残差空间相关检验。
- 空间效应：SAR/SDM/SAC 的 direct、indirect、total effects，支持精确矩阵迹与大样本近似，并输出计算方法。
- 权重矩阵：支持 row-standardized、binary、k-nearest、distance-band，并记录权重构造元数据。

App 必须展示空间参数、效应分解、Moran 指标、LM 检验、权重摘要；当某指标不可计算时展示结构化原因，而不是隐藏。

#### 2.1 GWR / GTWR 专项施工要求

GWR / GTWR 不应复用 SAR/SEM 的 `spatial_weights_path` 作为必需输入。SAR/SEM/SDM/SAC 依赖空间权重矩阵，GWR/GTWR 依赖坐标距离和局部核权重，两者应分开建模与验证。

新增模型类型：

- `spatial_gwr`：地理加权回归。
- `spatial_gtwr`：地理时空加权回归。

CLI 建议：

```text
gwr y x1 x2, coords(lon lat) kernel(gaussian) bandwidth(cv) adaptive(true)
gtwr y x1 x2, coords(lon lat) time(year) kernel(bisquare) bandwidth(cv) time_scale(auto)
```

`ModelSpec` 新增字段：

- `spatial_coord_columns`：长度为 2 的数组，如 `["lon", "lat"]` 或 `["x", "y"]`。
- `spatial_distance`：`euclidean`、`haversine`、`projected`。
- `spatial_crs`：可选 CRS 字符串；经纬度坐标未声明距离口径时必须 warning。
- `gwr_kernel`：`gaussian`、`bisquare`。
- `gwr_bandwidth`：数值带宽；与 `gwr_bandwidth_selection` 互斥。
- `gwr_bandwidth_selection`：`cv`、`aicc`。
- `gwr_adaptive`：布尔；`true` 表示带宽为邻近样本数，`false` 表示距离带宽。
- `gtwr_time_column`：GTWR 必填时间列。
- `gtwr_time_scale`：数值或 `auto`，用于平衡空间距离与时间距离。

shapefile / GeoPackage 不是 GWR/GTWR 的必需输入。第一优先级应支持 CSV 坐标列；第二阶段再增加：

- `geometry_path`：可选 `.shp`、`.gpkg` 或其他矢量文件路径。
- `geometry_id_column`：几何文件与主数据 join 的 ID。
- `geometry_point_policy`：`centroid`、`representative_point`、`point_geometry`。

当用户输入 polygon shapefile 时，Core 必须明确说明采用 centroid 还是 representative point。App 不得在前端计算几何中心；几何解析与坐标生成属于 Core 或专门数据处理包。

GWR 输出必须包含：

- `local_coefficients`：按观测输出每个变量的局部系数。
- `local_stderrors`、`local_statistics`：可计算时输出局部标准误与 t 值。
- `local_r2`、`fitted`、`residual`。
- `bandwidth`、`bandwidth_selection_score`、`kernel`、`adaptive`、`distance_metric`。
- `hat_diag`、`effective_parameters`、`sigma2`、`aicc`。
- 结构化 warning：带宽过小、局部设计矩阵奇异、坐标 CRS 不明、重复坐标、时间尺度不可识别。

GTWR 输出在 GWR 基础上增加：

- `time_scale`、`spatiotemporal_distance_summary`。
- 时间列有效范围、重复时空点数量。
- GTWR 带宽选择的 CV/AICc 轨迹摘要。

GWR / GTWR 验收测试：

- Julia 包测试覆盖 fixed bandwidth、adaptive bandwidth、Gaussian/Bisquare kernel、局部矩阵奇异降级。
- Runtime 垂直测试覆盖 `spatial_gwr` 与 `spatial_gtwr` 的字段校验和成功响应。
- App parser/render 测试覆盖 `gwr`、`gtwr` 命令和局部系数摘要展示。
- golden value 对齐优先选择 R `GWmodel::gwr.basic` / `gtwr` 或 Python 对应实现；若外部工具版本不可用，必须保存固定数据和手算小样本局部 WLS 结果。

### 3. 动态面板 GMM 成熟化

`MetricaPanel.jl` 动态面板必须达到 Arellano-Bond / Blundell-Bond 常用工作流标准：

- Difference GMM 与 System GMM 均为正式可用模型。
- 支持 instrument lag range、collapsed instruments、level/difference 方程工具变量配置。
- 输出 AR(1)、AR(2)、Sargan、Hansen、Difference-in-Hansen、instrument count、group count、time periods、dropped observations。
- 对 instrument proliferation、弱工具变量、组数不足、时间维度不足返回结构化 warning。
- 提供 `tidy` 系数表、系统级 `glance`、工具变量摘要和诊断解释。

测试对齐 R `plm::pgmm` 或公开 Stata 示例，禁止只验证字段存在。

### 4. ARCH/GARCH 波动模型族成熟化

`MetricaTimeSeries.jl` 需要从 ARCH/GARCH 基础估计扩展为波动建模族：

- 均值方程支持 constant、AR、ARMA、ARX。
- 方差模型支持 ARCH、GARCH、GJR-GARCH、EGARCH；后续预留 APARCH/FIGARCH。
- 误差分布支持 Gaussian、Student-t、skewed-t。
- 输出参数标准误、z 值、p 值、loglik、AIC、BIC、持久性、无条件方差。
- 支持 volatility forecast、mean forecast、VaR、ES、Kupiec VaR backtest。
- 输出收敛状态、优化器、梯度/Hessian 状态；Hessian 不可逆时不得伪造标准误。

测试对齐 R `rugarch` 或 Python `arch` 的固定数据结果。

### 5. Duration / Survival 模型族成熟化

`MetricaDuration.jl` 从 Cox 基础模型扩展为 survival 模型族：

- Cox PH 支持 ties 方法、strata、cluster robust SE、case weights。
- 输出 Schoenfeld residuals、PH global test、变量级 PH test、baseline hazard、baseline survival。
- 支持 time-varying covariates 的 counting-process 数据格式。
- 新增 parametric AFT：Weibull、Exponential、Log-normal、Log-logistic。
- 输出 hazard ratio、time ratio、置信区间、删失摘要、事件率、风险集摘要。
- App 展示 PH 诊断、删失摘要、baseline survival，并解释违反 PH 假设的 warning。

测试对齐 R `survival::coxph`、`cox.zph`、`survreg`。

### 6. GMM / System / Quantile / Nonlinear 模型族补强

GMM:

- 从线性 IV-GMM 扩展为可定义 moment conditions 的通用 GMM。
- 支持 one-step、two-step、iterated GMM、CUE。
- 输出 J test、C-stat、Difference-in-Hansen、弱工具变量诊断、稳健协方差。

System:

- SUR、2SLS、3SLS 增加跨方程约束、系统级 Wald/LR/LM test、robust/cluster covariance。
- 支持方程级与系统级预测、残差相关矩阵、约束报告。

Quantile:

- 支持多 tau 同时估计、bootstrap SE、rank/sparsity inference、quantile process 摘要。
- 增加 IV quantile 与 panel quantile 的协议与实现计划，避免混入普通 QR。

Nonlinear / Threshold:

- NLS 支持用户公式、解析/数值 Jacobian、参数协方差、置信区间、多起点策略。
- Threshold 支持单/多阈值、bootstrap threshold CI、sup-Wald、样本分段诊断。
- 所有非线性模型输出收敛、局部最优风险、起点摘要。

### 7. Bayesian 模型族正式建设

创建或完善 `MetricaBayes.jl`，不再停留在文档占位：

- 第一阶段实现 Bayesian linear regression 的 Normal-Inverse-Gamma 共轭模型。
- 第二阶段支持 Bayesian logistic/probit 与层级线性模型。
- 输出 posterior mean、posterior sd、credible interval、posterior predictive summary。
- 若引入 MCMC，必须同步实现 R-hat、ESS、trace summary、divergence warning。
- Runtime/App 只有在 Core 可用且测试通过后才暴露 Bayes 模型入口。

### 8. App 展示打磨

App 结果页按模型族展示专业诊断，而不是通用表格堆叠：

- 空间模型：权重摘要、空间参数、效应分解、Moran/LM 检验、GWR/GTWR 局部系数摘要、带宽选择结果、局部 R² 分布。
- 动态面板：工具变量数量、AR/Hansen 诊断、instrument proliferation warning。
- 波动模型：条件波动率、forecast、VaR/ES、分布假设。
- Survival：删失摘要、PH 检验、baseline survival。
- Quantile：多分位系数路径与置信带。
- Bayesian：后验摘要、可信区间、预测分布、采样诊断。

所有展示只消费 Core 结构化结果，不解析文本摘要。

### 9. 文档与教学体验

更新根目录 S5 总计划、`docs/architecture/runtime-protocol.md`、相关教程和示例数据说明。每个模型族文档必须包含：

- 模型族边界与适用场景。
- 模型公式与估计方法。
- 输入字段、权重/面板/时间/删失数据要求。
- 输出字段、诊断指标和解释。
- 常见 warning 的教学解释。
- 与 R/Python/Stata 常见工具的结果对齐说明。

文档正文、源码注释和 Julia docstring 均使用简体中文。

## Test Plan

- 每个 S5 package 必须有 package-level deterministic tests、golden value tests、failure-mode tests。
- Runtime vertical tests 必须检查关键数值、warning code、capability flags，而不是只检查 key 存在。
- App tests 必须覆盖“指标已计算”“指标不可计算但有原因”“模型族专属展示”三类状态。
- Cross-tool validation 至少覆盖：
  - Spatial: PySAL/spreg、R spatialreg/spdep。
  - Dynamic panel: R plm 或公开 Stata 示例。
  - GARCH: R rugarch 或 Python arch。
  - Survival: R survival。
  - Quantile: R quantreg。
- 验收前运行相关 Julia package tests、Runtime `cargo test`、App build/test；无法运行时必须记录原因和剩余风险。

## Assumptions

- 本方案目标是 S5 全量成熟化，不再接受“最小闭环”作为完成定义。
- 可以按依赖顺序施工和提交，但每个模型族的最终验收标准是完整模型族能力。
- 新增模型必须遵守 Core/Runtime/App 分层，禁止 UI 或 Runtime 承担计量计算。
- 任何暂未实现能力必须显式标记为 `planned`，并从可调用入口中隐藏。
- 数值可信度优先于功能数量；没有测试和诊断解释的模型不算成熟完成。
