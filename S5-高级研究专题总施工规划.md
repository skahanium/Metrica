# S5 高级研究专题总施工规划

> **定位：** 本文件是 Metrica `S5` 阶段的总施工规划，用于指导后续人类开发者与 AI 编程助手按统一路线推进高级研究专题建设。  
> **阶段方法：** 采用“总规 + 分期”方式推进；每个分期都必须形成 `Core -> Runtime -> App -> Output -> docs/tests` 的最小闭环。  
> **语言规则：** 本文件正文使用简体中文；协议字段名、模型类型、命令、路径和代码标识符保留英文。  
> **当前产品事实（与文档同步）：** 桌面可执行体为 **`tao` + `wry`** 宿主（`apps/metrica-desktop/src-tauri`），前端为 **`src-react`**；**研究与复现主路径为 CLI-first 命令消息流**（命令解析 → `runtimeClient` → Runtime HTTP → 消息流结构化渲染）。详细见 [`docs/architecture/app-shell.md`](docs/architecture/app-shell.md)。**不得**将历史上假设的「Tauri + `invoke` + 多面板表单工作台」当作当前实现撰写验收或施工前提。

## 1. 阶段目标

`S5` 的目标不是一次性堆满高级模型列表，而是在 `S1-S4` 已建立的结构化协议、Runtime 桥接、**CLI-first** 桌面消息流（`tao` + `wry` + `src-react`）与输出系统基础上，逐步建设高级研究专题，并同步清理文档体系，使后续施工有清晰、可信、可验证的依据。

`S5` 覆盖路线图中的全部高级专题，但分层推进：

- 优先交付：一般 GMM、动态面板 GMM、SUR / 联立方程、分位数回归。
- 后续交付：非线性 / 门限、ARCH / GARCH、空间计量、久期模型。
- 预研保留：非参数 / 半参数、贝叶斯能力。

## 2. 全阶段施工规则

- 每个 `S5` 专题进入代码前，必须先更新本总规划、对应 `S5.x` 实施计划和结构化结果边界。
- 新模型不得写入 `MetricaBase.jl`。`MetricaBase.jl` 只增加共享协议、抽象类型和结构化结果约定。
- Runtime 只做 schema 校验、路径解析、Julia 调用和响应搬运，不实现计量逻辑。
- App 只消费结构化结果，不解析 `summary()` 文本，不私自计算统计量。
- 每个专题必须有最小教学数据、CLI 主路径、Runtime 垂直切片、Julia 包测试、结构化 warning / error 清单和教程。
- 每个分期完成后必须同步清理文档，不允许旧计划长期留在活跃目录造成误导。
- 不开放任意 Julia 代码、任意 shell 命令、任意用户自定义 moment 函数 DSL，除非后续阶段另行设计受控模板机制。

## 3. S5.0 文档治理与阶段基线

### 目标

先把文档体系整理成可指导 `S5` 的状态，避免后续 AI 按**已废止的 UI 叙事**（例如多面板表单工作台、顶栏「运行模型」为主路径、Tauri `invoke` 假接口）、**过期路线图对照结论**或**已删除的旧 plan** 施工。

### 实施内容

- 更新 `docs/roadmap/s5-advanced-research-topics.md`，写明 `S5` 分期、优先级、验收口径和非目标。
- 更新主设计，明确当前事实基线：`S1-S4` 主链路已在 **CLI-first 消息流工作台**（`tao` + `wry` + `src-react`）上贯通，`S5` 在此之上扩展。
- 更新 `docs/architecture/app-shell.md`，**仅描述当前宿主与界面事实**：`metrica://` 原生桥、Runtime HTTP、消息流与命令行；多面板 IDE、Tauri 插件体系等若出现，只能作为 **`S6` 产品化候选** 或愿景，不得写成本期已实现主线。
- 更新 `docs/architecture/runtime-protocol.md`，补齐当前 Runtime 端点、`S4` 模型族、`S5` 扩展 schema 规则，移除或标注未实现能力。
- 将已完成旧 `docs/superpowers/specs/`、`docs/superpowers/plans/` 中仍有效结论并回权威文档，然后删除原活跃文件。
- 删除或并回根目录遗留的 `ui-project-button-system-plan.md`（仓库中已删除则本条视为已完成）。
- 更新 `README.md`、`apps/metrica-desktop/README.md`、`runtime/metrica-runtime/README.md`、`CLAUDE.md` 中过期阶段说明。

### 验收标准

- `docs/superpowers/specs/` 与 `docs/superpowers/plans/` 只保留当前 `S5` 活跃设计和计划。
- 全仓库**不得**把下列**已失效或片面**的说法当作**当前事实**写进活跃文档（允许在历史快照、对照说明中标注「某日期前曾成立，现已推翻」）：例如「`tutorials/` 恒为空」「`S3` export/compare 与桌面完全未接」「表单 + 顶栏运行模型是唯一主路径」「桌面栈为 Tauri 2 + `invoke`」等；若需记录缺口，须写明**核验日期**与**所依据代码路径**。
- 文档边界清楚：路线图管阶段，architecture 管协议与局部结构，spec / plan 管当前活跃施工。

## 4. S5.1 一般 GMM 与过识别检验

### 目标

建设 `S5` 的高级估计底座，优先服务线性 IV-GMM 和后续动态面板。

### 拟新增能力

- Julia 包：`packages/MetricaGMM.jl`
- 模型类型：`gmm_linear`
- CLI：`gmm y x1 x2, endogenous(x2) instruments(z1 z2) weight(two_step)`
- Runtime：`model_spec.model_type = "gmm_linear"`
- App：结构化展示 GMM 摘要、系数表、过识别诊断、权重矩阵说明。

### 最小范围

- 只做线性 IV-GMM。
- 支持 one-step 与 two-step 权重矩阵。
- 输出 Hansen / Sargan J、过识别自由度、p 值、moment 数、参数数、迭代信息。
- 对欠识别、工具变量不足、权重矩阵奇异、弱工具变量风险给结构化 error / warning。
- 不开放任意 moment 函数，不执行用户 Julia 代码，不做非线性 GMM。

### 验收标准

- Julia 包测试覆盖正常、欠识别、过识别、奇异权重矩阵、弱工具变量提示。
- Runtime 垂直切片从 HTTP 请求返回 `glance`、`tidy`、`diagnostics`、`warnings`。
- App CLI 能运行 `gmm`，并在结果流展示结构化诊断。
- 教程说明 GMM 与 2SLS 的关系、J 检验含义和常见误用。

## 5. S5.2 动态面板 GMM

### 目标

基于 `S5.1` 的 GMM 底座实现动态面板主路径。

### 拟新增能力

- 所属包：优先放入 `MetricaPanel.jl`，依赖 `MetricaGMM.jl`
- 模型类型：`dynamic_panel_gmm`
- CLI：`xtabond y L.y x1, id(firm) time(year) lags(2 4)`
- 结构化诊断：AR(1)、AR(2)、Hansen / Sargan、工具变量数量、个体数、时间期数。

### 最小范围

- 先做 Difference GMM。
- System GMM 作为本期扩展项，只有 Difference GMM 稳定后进入。
- 支持明确 lag 区间与工具变量折叠选项。
- 对短面板、工具变量过多、时间期不足、缺失导致样本变化给结构化 warning / error。

### 验收标准

- 确定性小样本测试验证 lag 构造、差分样本数、工具变量矩阵形状。
- 垂直切片通过 Runtime 与 App CLI。
- 教程解释 Nickell bias、AR(2) 检验和工具变量过多风险。
- 不把动态面板作为普通 `panel` 分支的隐藏选项。

## 6. S5.3 SUR 与联立方程

### 目标

覆盖多方程系统中的高频研究需求。

### 拟新增能力

- Julia 包：`packages/MetricaSystem.jl`
- 模型类型：`sur`、`system_2sls`、`system_3sls`
- CLI：`sur (y1 x1 x2) (y2 z1 z2)`；`reg3 (...) (...) , endogenous(...|...) instruments(...|...) method(3sls)`（`|` 与方程块一一对应；默认 `system_2sls`，`method(3sls)` 为 `system_3sls`）。协议与教程见 [`docs/architecture/runtime-protocol.md`](docs/architecture/runtime-protocol.md) 专节、[`tutorials/s5-sur-system.md`](tutorials/s5-sur-system.md)。
- 结构化结果：方程级 `glance`、合并 `tidy`、残差协方差矩阵、方程相关诊断。

### 实施顺序

- 先做 SUR。
- 再做联立方程 2SLS。
- 最后在 GMM / 系统方程基础稳定后做 3SLS。

### 验收标准

- 每个方程结果可单独展示，也可形成合并系数表。
- 方程间协方差和相关性作为结构化诊断返回。
- App 不把多方程结果压成文本块。

## 7. S5.4 分位数回归

### 目标

支持条件分布异质性分析。

### 拟新增能力

- Julia 包：`packages/MetricaQuantile.jl`
- 模型类型：`quantile`
- CLI：`qreg y x1 x2, quantile(0.5)`
- 结构化结果：分位点、系数表、伪 R²、迭代状态、可选多分位点对比。

### 最小范围

- 支持单一分位点。
- 多分位点作为扩展，必须返回结构化数组而非拼文本。
- 标准误先采用可解释的基础口径；bootstrap 若进入，必须有明确随机种子与重复次数记录。

### 验收标准

- 覆盖中位数回归、极端分位点样本不足、奇异设计矩阵。
- 教程解释“均值效应”和“分位点效应”的差异；首期单 \(\tau\) 垂直切片见 [`docs/superpowers/plans/2026-05-16-s5-execution-plan.md`](docs/superpowers/plans/2026-05-16-s5-execution-plan.md) 专节与 [`tutorials/s5-quantile-regression.md`](tutorials/s5-quantile-regression.md)。
- App 能展示同一变量在不同分位点的系数变化。

## 8. S5.5 非线性、门限、非参数与半参数

### 目标

建设更灵活的函数形式能力，但保持受控接口。

### 拟新增能力

- Julia 包：`packages/MetricaNonlinear.jl`
- 模型类型：`nls`、`threshold`
- 非参数 / 半参数先作为受控候选，不开放任意函数脚本。

### 实施顺序

- 先做 NLS 的受控参数表达式与收敛诊断。
- 再做门限回归，输出门限值、区制样本量、区制系数。
- 非参数 / 半参数只进入预研和接口预留，待教学数据、输出结构和测试口径明确后实现。

### 验收标准

- 非线性优化必须返回收敛状态、初值、迭代次数、失败原因。
- 门限模型必须报告每个区制样本量，样本过少时阻断或警告。
- 不允许用户通过自由 Julia 代码定义任意模型。

### 首期落地（与实现对齐，2026-05）

- **包与注册：** `packages/MetricaNonlinear.jl`；`MODEL_REGISTRY["nls"]`、`["threshold"]`。
- **NLS：** `model_type: "nls"`；白名单 `nls_family`（首期仅 `exp_growth`）；必填 `nls_start`（长度 3）；**Optim.jl + Nelder-Mead**；`diagnostics` 含收敛、迭代、目标值、`start_used` 等；CLI：`nls y x, start(β1 β2 β3)`。
- **门限：** `model_type: "threshold"`；`threshold_variable` + 单调 `threshold_grid`（2–500）；`threshold_trim_frac` 默认 0.1；各区制样本 \<10 时搜索失败并结构化报错；CLI：`threg ... , qvar(q) grid(min max n)`（`threg` 为 App 动词）。
- **非参数 / 半参数：** 仅协议与 `runtime-protocol.md` 预留说明；未实现独立 `model_type` 前须返回 `RUNTIME_UNSUPPORTED_MODEL_TYPE`（见该文件 5c 小节）。
- **执行细节与 Task 表：** [`docs/superpowers/plans/2026-05-16-s5-execution-plan.md`](docs/superpowers/plans/2026-05-16-s5-execution-plan.md)「S5.5」专节；教程 [`tutorials/s5-nonlinear-threshold.md`](tutorials/s5-nonlinear-threshold.md)。

## 9. S5.6 ARCH / GARCH

### 目标

在既有 `MetricaTimeSeries.jl` 上扩展金融与宏观时间序列常用波动率模型。

### 首期落地（与实现对齐，2026-05）

- **包与路径：** `packages/MetricaTimeSeries.jl`；`MODEL_REGISTRY["arch"]`、`["garch"]`（与 ARIMA 一致注册）；Runtime 走 **`julia_time_series_bridge_entry.jl`**（与 `arima` 同桥）。
- **ARCH：** `model_type: "arch"`；必填 `variable`、`time_column`、`arch_order`（1–12）；**Optim.jl + 手写 Gaussian QMLE**；`diagnostics` 含收敛、迭代、对数似然、持久度、条件波动预览等；CLI：`arch y, time(date) arch(q)`。
- **GARCH：** `model_type: "garch"`；默认 GARCH(1,1)（`garch_p`/`garch_q` 可省略）；阶数上界与 Runtime 一致（\(p,q\le 5\)，\(p+q\le 8\)）；CLI：`garch y, time(date) [arch(p)] [garch(p q)]`，并与示例等价写法 `garch y, time(date) arch(1) garch(1)`（表示 \(p=1,q=1\)）。
- **数据：** 教学演示 `datasets/demo/garch_demo.csv`；教程 [`tutorials/s5-arch-garch.md`](tutorials/s5-arch-garch.md)。
- **执行细节与 Task 表：** [`docs/superpowers/plans/2026-05-16-s5-execution-plan.md`](docs/superpowers/plans/2026-05-16-s5-execution-plan.md)「S5.6 ARCH / GARCH」专节。

### 拟新增能力

- 所属包：`MetricaTimeSeries.jl`
- 模型类型：`arch`、`garch`
- CLI：见上「首期落地」；结构化结果见 `runtime-protocol.md` 专节。

### 最小范围

- GARCH(1,1) 为默认；支持协议内 **有上界** 的 \(p,q\) 扩展。
- ARCH(q) 与 GARCH(p,q) 首期均为**常数均值**；不在本期做完整金融风险系统或 VaR 产品。

### 验收标准

- 覆盖正方差约束、收敛失败、样本过短（Julia + Runtime + App Vitest）。
- 输出可用于图表展示条件波动率，但图表本身消费结构化字段（`conditional_volatility_preview` 等）。
- 教程说明 volatility clustering 和模型限制。

## 10. S5.7 空间计量

### 目标

为应用研究中的空间依赖提供受控能力。

### 拟新增能力

- Julia 包：`packages/MetricaSpatial.jl`
- 模型类型：`spatial_lag`、`spatial_error`
- CLI：`spreg y x1 x2, spatial_weights("w.csv") id(region) model(lag)`；**兼容** `spatial_id(region)` 与简写 `weights("w.csv")`（**仅** `spreg` 下表示空间权重边表，非回归频数权重）。
- 结构化结果：空间权重摘要、Moran 相关诊断、空间参数、直接 / 间接效应预留。

### 最小范围

- 先支持外部权重矩阵文件。
- 先做 SAR / SEM 主路径。
- 不做地图编辑器，不在 App 中手工绘制空间权重。

### 验收标准

- 权重矩阵维度、ID 对齐、行标准化状态必须结构化报告。
- ID 不匹配、孤立单元、非方阵权重矩阵必须返回明确错误。
- 教程说明空间权重不是普通控制变量。

## 11. S5.8 久期模型

### 目标

支持事件发生时间分析。

### 拟新增能力

- Julia 包：`packages/MetricaDuration.jl`
- 模型类型：`duration_cox`，后续扩展参数模型。
- CLI：`stcox time event x1 x2`（前两列映射为 `duration_time_column` / `duration_event_column`，公式固定为 `ph ~ x1 + x2`）。
- 结构化结果：风险比、基准风险摘要、删失比例、事件数、比例风险诊断预留。

### 最小范围

- Cox PH 主路径。
- 参数久期模型作为后续扩展。
- 不做复杂生存曲线编辑器。

### 验收标准

- 事件列、时间列、删失、零或负时间必须明确校验。
- 输出默认展示 hazard ratio 与置信区间。
- 教程解释删失与风险比，不把结果解释成线性回归系数。

## 12. S5.9 贝叶斯能力预研与最小闭环

### 目标

为贝叶斯计量保留结构化接口，但避免提前变成 MCMC 平台。

### 拟新增能力

- Julia 包候选：`packages/MetricaBayes.jl`
- 模型类型候选：`bayes_linear`
- CLI 候选：`bayesreg y x1 x2`
- 结构化结果：posterior mean、credible interval、采样摘要、收敛诊断。

### 最小范围

- 只考虑贝叶斯线性回归。
- 不开放任意概率模型语言。
- 若采样依赖过重或打包风险过高，本期只完成设计和小样本 proof-of-concept。

### 验收标准

- 每次运行记录随机种子、采样次数、warmup、链数。
- 输出必须包含收敛诊断，不能只给 posterior mean。
- 教程明确 credible interval 与 frequentist CI 的差别。

## 13. 统一公开接口规则

- 所有新模型继续走 `fit_model` 信封。
- `model_spec.model_type` 使用明确枚举，不用自由字符串解释模型。
- 所有新结果必须返回 `glance`、`tidy`、`diagnostics`、`warnings`。
- 多方程、多分位点、多链后验等复杂结果使用结构化数组，不拼接为展示文本。
- CLI 只是生成结构化 `ModelSpec` 的入口，不是独立业务逻辑层。

## 14. 统一测试规则

- 每个 `S5.x` 至少包含 Julia 包测试、Runtime 垂直切片、App parser / client / render 测试、教程命令冒烟检查。
- 所有数值模型必须有确定性小样本测试，优先验证结果形状、识别条件、错误路径和关键统计量。
- 涉及随机或优化的模型必须记录 seed、迭代状态、收敛状态，并测试失败路径。
- 文档变更必须运行引用检查，避免删除旧文档后留下悬空链接。
- 阶段完成不得只用窄测试宣称全阶段完成；完成说明必须列出已验证范围和未覆盖风险。

## 15. 阶段假设

- `S5` 按研究频率排序推进：GMM、动态面板、系统方程、分位数、非线性 / 门限、GARCH、空间、久期、贝叶斯。
- `S5` 不追求一次性实现所有高级模型的完整理论宇宙；每个专题先做最小可信闭环。
- `S6` 打包产品化（安装包形态与宿主技术栈选型等，见 `docs/roadmap/s6-packaging-and-productization.md`）与 `S7` AI 助手不并入 `S5`。
- 旧文档采用“并回后删除”，不新建长期 archive，除非后续用户明确改变文档策略。
