# Metrica 总体蓝图

## 1. 文档定位

本文件是 Metrica 的**总蓝图**。它只回答四类问题：

1. Metrica 到底要做成什么
2. 为什么这样分层、这样排路线
3. 当前长期阶段体系是什么
4. 各类文档分别负责什么，不负责什么

本文件**不是**：

- 当前冲刺计划
- 某个里程碑的详细设计
- Runtime 字段级 schema
- App 组件级实现说明

如果需要看当前仍在生效的局部设计，应转到：

- `docs/roadmap/`：阶段级路线
- `docs/architecture/runtime-protocol.md`：Runtime 协议
- `docs/architecture/app-shell.md`：App 工作台结构
- `docs/superpowers/specs/2026-04-30-metrica-main-design.md`：当前主设计锚点

---

## 2. 项目定位

### 2.1 Metrica 是什么

Metrica 是一个面向教学与常见应用研究的现代计量工作台。  
它不是“Julia 版 Stata 命令壳”，也不是“把若干估计器拼起来的包集合”。

Metrica 的目标是把以下几件事统一起来：

- 结构化计量结果
- 可教学的默认行为
- 明确的 Core / Runtime / App 分层
- 可扩展但受控的桌面工作流

### 2.2 服务对象

首要服务对象：

- 经济学、金融学、管理学学生
- 教师与课程助教

兼顾对象：

- 做常见实证研究的研究人员

不把以下对象作为当前优先目标：

- 追求命令生态完全兼容的 Stata 重度用户
- 需要开放脚本平台的高级自动化用户
- 需要云协作、多租户和团队平台能力的用户

### 2.3 核心成功标准

Metrica 的成功标准不是“模型列表最多”，而是以下几点同时成立：

- **一致**：不同模型共享统一接口与结果语义
- **可靠**：关键结果有稳定测试和黄金样例，不靠手工解释补锅
- **易学**：错误、警告、默认输出和示例对学生友好
- **可演进**：后续模型、报告、项目系统、AI 增强能接在同一协议上
- **分层清楚**：Core、Runtime、App 各做各的，不互相吞职责

---

## 3. 产品边界

### 3.1 明确要做的事

Metrica 长期要形成的是一套完整但克制的计量工作台能力：

- 数据导入、检查、基础处理
- 常见线性与面板模型
- 高频应用研究模型
- 结构化结果、诊断、导出与报告
- 项目文件、运行记录、复现工作流
- 在结构化对象稳定后引入受控 AI 增强

### 3.2 明确不做的事

当前路线明确不追求：

- 复刻完整 Stata 命令生态
- 开放任意 Julia / shell / Python 脚本入口
- 插件市场或第三方扩展平台
- 云协作、多用户平台
- 与 Stata / R / Python 的全量系统互验宇宙

这里的原则不是“永远不可能做”，而是：  
**这些都不属于当前产品战略的主路径，不能反过来主导架构。**

---

## 4. 总体架构

Metrica 采用三层主架构：

### 4.1 Core

位于 `packages/`，负责：

- 计量协议
- 结构化结果
- 估计器与诊断
- 数据操作
- 输出对象

Core 的原则：

- 不依赖 GUI
- 不把第三方对象直接暴露给 Runtime 或 App
- 公开结果必须是结构化对象，而不是解析文本

### 4.2 Runtime

位于 `runtime/`，负责：

- 接收结构化请求
- 调用 Julia 执行真实任务
- 管理进程、会话、日志、取消、错误传递
- 将结果搬运为稳定协议

Runtime 的原则：

- 不重做计量逻辑
- 不解释模型结果
- 不成为第二套业务逻辑层

### 4.3 App

位于 `apps/`，负责：

- 工作台交互
- 数据与模型配置
- 结构化结果展示
- 导出与项目工作流入口

App 的原则：

- 不解析 `summary()` 文本
- 不私自计算统计量
- 不绕过 Runtime 直接调用 Core

---

## 5. 包结构原则

Metrica 采用 monorepo，多包协作，但每个包职责单一。

当前与长期的主包方向如下：

- `MetricaBase.jl`：协议内核
- `MetricaLinear.jl`：线性模型族
- `MetricaPanel.jl`：面板模型族
- `MetricaDiagnostics.jl`：诊断能力
- `MetricaData.jl`：数据操作
- `MetricaOutput.jl`：输出与导出对象
- `MetricaDiscrete.jl`：离散 / GLM（远期）
- `MetricaCausal.jl`：因果推断（远期）
- `MetricaTimeSeries.jl`：时间序列（远期）
- `Metrica.jl`：元包

### 5.1 `MetricaBase.jl` 的边界

`MetricaBase.jl` 只放：

- 抽象类型
- 统一接口
- 结果语义
- 错误 / 警告 / 教学提示
- 能力发现与协议边界

`MetricaBase.jl` 不放：

- 具体估计器实现
- GUI 逻辑
- Runtime 逻辑
- 输出渲染细节

一句话说，Base 是**协议内核**，不是功能垃圾场。

---

## 6. 结果语义原则

Metrica 面向外部消费的结果必须是结构化对象。

核心结果层包括：

- `glance`：模型级摘要
- `tidy`：参数级长表
- `augment`：观测级增强结果
- `warnings`：结构化警告
- `diagnostics`：结构化诊断
- `artifacts`：导出与结果产物

这条原则直接约束三层：

- Core 必须产出结构化对象
- Runtime 只搬运结构化对象
- App 只消费结构化对象

禁止倒退到：

- 解析 `summary()` 文本
- 用打印输出驱动业务逻辑
- 把“当前能看懂”误当作“协议已经成立”

---

## 7. 教学友好原则

教学友好不是 UI 润色，而是产品约束。

Metrica 必须把以下内容视为一等需求：

- 缺失值处理可见
- 样本量变化可见
- 变量角色清楚
- 错误和警告可读
- 默认输出可以直接用于教学讨论

这意味着：

- “删了多少行、为什么删”不能静默发生
- “诊断不可用”必须说明原因
- “模型设定有风险”必须给出面向学生的解释

---

## 8. 阶段划分原则

Metrica 的阶段不是按教材目录机械切，也不是按“哪个模型听起来更高级”来排。

阶段划分统一依据四个标准：

### 8.1 必要性

优先覆盖最常用、最能支撑教学与常规研究的能力。

### 8.2 闭环能力

优先做能形成 `Core -> Runtime -> App -> Output` 闭环的能力。

### 8.3 依赖顺序

先做协议、结果语义、工作台地基，再扩张模型族。

### 8.4 长期实现难度

高复杂专题不能提前挤占高必要能力的建设节奏。

因此，路线排序原则是：

**必要性优先，兼顾闭环能力、依赖顺序和长期实现难度。**

---

## 9. 阶段体系

当前长期路线采用 `S1-S7`。

### `S1` 协议与工作台地基

目标：

- 建立稳定主链路
- 建立统一协议、统一结果、数据操作和基础工作台

覆盖方向：

- Base
- Runtime 基础协议
- App 基础工作流
- OLS/WLS、基础诊断、基础面板、数据操作

### `S2` 核心实证工作台

目标：

- 覆盖最常用、最必要的线性与面板能力闭环

覆盖方向：

- IV/2SLS
- GLS/FGLS
- 稳健 / cluster 协方差
- HDFE、CRE、Panel IV、升级面板诊断

### `S3` 复现与报告产品化

目标：

- 把“能算”升级为“能做完整项目”

覆盖方向：

- 模型表与导出
- 项目文件
- 运行记录
- 数据谱系
- 错误恢复

### `S4` 高频应用研究模型

目标：

- 覆盖教学和常见应用研究里高频的模型族

覆盖方向：

- GLM / 有限因变量
- 因果推断
- 基础时间序列
- 复杂抽样

### `S5` 高级研究专题 ✅ 已完成

目标：承载高复杂但长期重要的专题能力。

覆盖方向（全部已实现）：

- **GMM**：一步/两步/iterated/CUE、Hansen J、C-stat、Diff-Hansen、Cragg-Donald F
- **动态面板**：Difference GMM + System GMM（Blundell-Bond）、collapsed instruments
- **SUR / 联立方程**：SUR(FGLS)、system 2SLS/3SLS、Wald/LR/LM、robust sandwich cov
- **分位数回归**：多 τ、bootstrap SE、rank score、sparsity、IV quantile
- **非线性 / 门限**：exp_growth/logistic/power/gompertz、数值 Jacobian/CI、多起点、多门限、bootstrap CI、sup-Wald
- **ARCH / GARCH / GJR / EGARCH**：Gaussian/Student-t/skewed-t、forecast/VaR/ES/Kupiec
- **空间计量**：SAR/SEM/SDM/SDEM/SAC/SLX/GWR/GTWR/Probit、Moran/LM、kNN/distance-band
- **久期**：Cox PH（Efron/strata/cluster/weights/counting-process）、Schoenfeld/PH、AFT 四分布
- **贝叶斯**：NIG 共轭 + MCMC(R-hat/ESS)、logistic/probit、层级线性模型
- **统一协议**：全部包实现 model_capabilities + augment_status + WARNING_CODE

### `S6` 安装包与产品化收口

目标：

- 将桌面端从"开发者可运行"提升为"终端用户可安装、可恢复、可演示"

覆盖方向：

- 安装包与分发形态（当前 Alpha 为 tao+wry；本阶段选定并落地目标打包栈）
- Julia 运行时捆绑或自动安装
- 崩溃恢复与用户提示
- 课堂/示例工程

### `S7` AI 增强与研究助理层

目标：

- 在结构化对象足够稳定后，引入受控 AI 增强

覆盖方向：

- 诊断解释
- 教学说明
- 报告草稿
- 下一步建议
- 受控动作预览与确认

---

## 10. 当前阶段判断

按当前仓库事实（2026-05-17）：

- `S1`–`S5`：全部完成 ✅
- `S6`：安装包与产品化收口（下一阶段主线）

当前仓库实现已涵盖 20 个 Core Julia 包、6 个 S5 模型族全量成熟化、统一能力协议、CLI-first App。下一步应聚焦打包分发与产品化。

---

## 11. 文档体系原则

当前文档体系应保持精简，只保留还承担职责的文档。

### 11.1 本文件负责什么

本文件负责：

- 总定位
- 总边界
- 总阶段体系
- 总架构原则

### 11.2 `docs/roadmap/` 负责什么

`docs/roadmap/` 负责：

- 各阶段目标
- 纳入能力
- 排除项
- 阶段验收口径

### 11.3 `docs/architecture/` 负责什么

`docs/architecture/` 负责：

- 仍生效的局部架构
- Runtime 协议
- App 工作台结构
- 文档边界规则

### 11.4 `docs/superpowers/specs/` 负责什么

只保留**当前仍在生效**的主设计或活跃设计。

已完成里程碑的设计文档和实施计划不应长期保留。  
它们一旦完成，就应该：

- 必要信息回并到总蓝图或当前主设计
- 原文档直接删除

---

## 12. 当前工作约束

后续所有实现和文档更新，都应服从以下约束：

1. 不新增平行主线  
2. 不为已完成阶段保留大量历史负担  
3. 不让 App、Runtime 或文档退回到文本解析思维  
4. 不为了模型数量牺牲结构清晰度  
5. 不把 AI 叙事提前压过 `S3` 和 `S4`

---

## 13. 一句话总结

Metrica 的正确路线不是“尽快把教材目录做满”，而是：

**先把结构化、可教学、可复现的现代计量工作台做稳，再在同一协议上扩展模型族、报告系统和 AI 增强。**
