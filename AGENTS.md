# Metrica AI 协作协议

本文件是本仓库内各类 AI 编程助手（包括但不限于 Codex、Claude、Cursor 及其他具备代理或补全能力的模型）的**项目级主指令源**。

若工具支持项目指令文件，本仓库应以 `AGENTS.md` 为权威来源。工具专属文件可镜像或改编本内容，但不得与之冲突。

## 1. 项目身份

Metrica 不仅是 Julia 包集合，而是**双轨产品**：

- `Metrica Core`：Julia 计量包与协议层
- `Metrica Runtime`：桌面软件与 Julia 执行之间的桥接层
- `Metrica App`：跨平台原生桌面工作台

所有变更须保持上述分层不被混淆。

## 2. 首要目标

项目目标是构建现代计量生态，具备：

- 教学友好
- 架构一致
- 数值可靠
- 可扩展
- 能演进为跨平台原生产品

不得以牺牲协议清晰度或架构稳定性为代价，换取短期功能数量。

## 3. 不可违背原则

在本仓库工作的所有 AI 助手均须遵守以下规则。

### 3.1 尊重架构

不得模糊以下边界：

- `packages/`：Julia 包代码与协议定义
- `runtime/`：桥接与执行管理逻辑
- `apps/`：桌面应用代码
- `docs/`、`tutorials/`、`datasets/`、`benchmarks/`：支撑资产，非核心实现逻辑

UI 代码不得包含计量逻辑。  
Core 包代码不得依赖 GUI 细节。  
Runtime 代码不得成为第二套业务逻辑层。

### 3.2 结构优先于功能数量

优先稳定接口、清晰边界与结构化输出，而非快速堆模型或页面。

犹豫时：

- 先冻结接口
- 实现最小可用垂直切片
- 端到端验证该切片

### 3.3 结构化结果优于展示文本

不得基于解析打印摘要来构建下游行为。  
凡可能被文档、笔记本、导出或桌面应用消费的结果，均须表示为结构化数据。

优先的公开结果概念包括：

- `glance`
- `tidy`
- `augment`
- 结构化警告
- 结构化诊断

### 3.4 教学体验是核心要求

不得将教学友好行为视为可选润色。  
缺失值处理、样本量变化、变量角色清晰度与可读警告均为**一等产品需求**。

### 3.5 禁止静默捏造

不得虚构：

- 未定义的 API
- 不存在的文件
- 未实际运行的基准或测试结果
- 不具备的产品能力

若存在不确定性，须在代码注释、文档或完成说明中**明确写出假设**。

### 3.6 注释与文档正文语言（简体中文）

- 仓库内 **`packages/`、`runtime/`、`apps/`、`scripts/*.jl`** 等**源代码**中的注释（含 `#`、`//`、`//!`、块注释）以及 Julia **文档字符串**（`"""..."""`）必须使用**简体中文**（`zh-CN`）。
- 以说明为主的配置文件（如 `.gitignore`、`.gitattributes`）中的分区说明注释，**宜**使用简体中文。
- **本文件及 `docs/` 下 Markdown 文档的正文**（标题、段落、列表说明）须使用**简体中文**；协议字段名、代码标识符、JSON 键名、命令行与仓库路径等可保持英文以便互操作。文档内**代码示例中的注释**仍须使用简体中文。

## 4. 必要工作顺序

除非用户明确要求极小的孤立编辑，否则遵循以下顺序。

### 阶段 1：弄清本地事实

在提议或修改之前：

- 阅读相关文件
- 阅读邻近文档
- 识别活跃子系统
- 与仓库既定方向对齐

若能直接读取，则不得猜测仓库结构。

### 阶段 2：先设计再实现

对任何实质性功能、架构变更、工作流变更或新子系统：

- 界定范围
- 识别边界
- 选择最小可行垂直切片
- 在广泛实现之前书面记录预期行为

若仓库已有设计或计划文档，应遵循之。  
若无，则在广泛实现之前先创建。

### 阶段 3：最小实现

实现满足当前里程碑的**最窄**变更。

避免：

- 投机式抽象
- 过早泛化
- 支线任务
- 与当前切片无关的大范围重构

### 阶段 4：验证后再宣称完成

在验证相关层之前，不得宣称任务已完成。

例如：

- 文档变更：核对路径、引用与一致性
- Runtime schema 变更：核对示例与命名一致性
- 包代码变更：在可行时运行针对性测试
- UI 变更：验证受影响流程或至少可构建结构

若无法验证，须如实说明。

### 阶段 5：让仓库更清晰

完成的变更应至少改进以下之一：

- 清晰度
- 结构
- 文档
- 测试覆盖
- 命名或边界一致性

## 5. 代理式流程规则

适用于长时间运行的代理与多步助手。

### 5.1 实质性工作优先书面计划

若变更触及多个目录、接口或子系统，应先在 `docs/` 中创建或更新书面计划。

### 5.2 假设须可见

未经用户确认而推进时，将假设记录在以下之一：

- 设计文档
- 实施计划
- 完成总结
- 适当的简短行内说明

### 5.3 不隐瞒权衡

若决策影响未来架构，应清楚陈述权衡，而非埋藏在代码中。

### 5.4 单一职责单元

优先小而聚焦的文件与模块。  
引入新代码时避免巨型混合职责文件。

## 6. 标签补全与小型行内建议规则

即使模型仅提供类补全建议，也适用以下规则。

### 6.1 匹配本地模式

补全须遵循当前目录已建立的命名、布局与风格。  
不得向无关代码注入新风格、框架模式或架构假设。

### 6.2 不得夹带架构变更

行内补全不得悄悄引入：

- 新分层
- 隐藏依赖
- 跨模块捷径
- 临时配置体系

除非用户明确要求更大范围，否则小建议须保持局部。

### 6.3 显式优于魔法

补全代码时：

- 选用可读名称
- 避免不透明辅助链
- 除非已建立，否则避免反射过重或过度「聪明」的模式

### 6.4 不留破损占位

不得插入：

- `TODO`
- `TBD`
- 虚假实现
- 占位返回值
- 类似「稍后实现」的注释

除非用户明确要求脚手架且知晓代码不完整。

## 7. 仓库特定方向

### 7.1 当前优先级

当前仓库方向为：

- 平台优先架构
- 直接多包布局
- 以 `Base -> OLS -> Output` 验证 alpha
- 桌面应用与 Runtime 并行规划

除非用户明确改变策略，否则工作应支持该方向。

### 7.2 对 Core 包的期望

`MetricaBase.jl` 应定义共享协议、类型、接口、结构化结果、模型框架语义与警告能力模式。

不得沦为以下内容的倾倒场：

- 估计量实现
- UI 逻辑
- 输出渲染内部
- 与协议层无关的笨重算法族

### 7.3 对 Runtime 的期望

`runtime/` 应聚焦：

- 进程编排
- 环境管理
- 请求/响应 schema 处理
- 日志与取消

不得重复属于 Julia 包的计量逻辑。

### 7.4 对 App 的期望

`apps/` 应聚焦：

- 项目工作区流程
- 数据导入与检查
- 模型配置
- 结果展示
- 导出
- 面向教学的解释

应消费结构化输出，而非解析终端文本。

## 8. 文档规则

任何重大架构或工作流变更均须更新相关文档。

优先位置：

- 设计文档：`docs/superpowers/specs/`
- 实施计划：`docs/superpowers/plans/`
- 子系统笔记：`docs/architecture/`

若代码变更使现有文档失效，在同一会话中更新文档（若可行）。

### 8.1 控制主文档数量

不得在每次阶段性收口时都新增一套并行的主 `spec` / `plan`。  
若当前工作仍属于既有阶段目标的细化、收口或执行，应优先：

- 更新现有主设计文档
- 更新现有主实施计划
- 在旧文档开头明确状态与适用范围

仅当工作已经形成**新的独立阶段**、且旧文档继续承载会造成职责混乱时，才允许新增新的主 `spec` / `plan`。

### 8.2 临时计划必须回收

若为短期收口、迁移或局部修正而创建临时计划文档，则在相应内容并回主计划后，应尽快删除该临时文档，避免文档体系长期分叉。

保留历史文档时，必须满足以下至少一项：

- 它代表一个已完成且独立的阶段
- 它对理解架构演化仍有明确参考价值
- 它已被清楚标注为历史草案，而非当前实施依据

### 8.3 文档精简约束（token 效率）

设计规格（spec）和实施计划（plan）必须控制篇幅，避免浪费 token。

**设计规格**只写：

- 架构决策和理由
- 类型定义与接口签名
- 非目标与边界
- 验收标准

**实施计划**只写：

- 文件路径清单（哪些新建、哪些修改）
- 每个 Task 的目标（一两句话）
- 关键代码变更描述（修改哪个函数/类型，改什么）
- 测试要点（验证什么断言）

**禁止**：

- 在 plan 中复制完整函数体或完整文件内容
- 在 spec 中重复 plan 的实施步骤
- 写"与 Task N 类似"但又不给出差异的冗余说明

执行者应从现有代码文件中读取模式，而非从文档中复制代码。文档只承载**决策和接口约定**，不承载**实现细节**。

## 9. 测试与验证规则

AI 助手应优先做针对性验证，而非完全不验证。

最低期望：

- 创建文档或脚手架后核对文件路径存在
- 核对协议示例的命名一致性
- 对变更代码在可行时运行针对性测试
- 避免用窄检查宣称广泛正确

对数值或计量代码，优先：

- 确定性测试
- 结果形状测试
- 在可行时做跨工具对齐测试
- 覆盖缺失数据、奇异性与样本变化等边界

## 10. 变更控制规则

不做无关编辑。  
除非用户明确要求，否则不重排大段无关表面。  
无必要不得重命名或移动文件。  
不得为简化任务而覆盖用户撰写的内容。

若遇到冲突的本地变更：

- 停止
- 保留已有工作
- 清楚说明冲突

## 11. 完成定义

仅当以下适用项全部为真时，任务才算完成：

- 变更符合仓库架构
- 已实现最小预定范围
- 已更新相关文档
- 已执行相关验证，或已说明限制
- 未在沉默中留下占位逻辑
- 结果可被下一位人类或代理理解

## 12. AI 助手输出风格偏好

汇报已完成工作时：

- 总结实际变更
- 说明已做验证
- 说明重要假设或剩余缺口
- 保持简洁具体

避免夸大信心。  
除非已验证到相应程度，否则勿称「生产就绪」。

## 13. 指令优先级

若存在多份指令文件，优先级为：

1. 用户直接请求
2. 仓库架构与活跃设计文档
3. 本 `AGENTS.md`
4. 工具适配文件（如 `CLAUDE.md`、`.cursorrules` 或编辑器设置）

工具专属文件应服务于与本文件兼容，而非覆盖项目方向。
