# Metrica AI 协作协议

本文件是本仓库内各类 AI 编程助手（Claude、Codex、Cursor 等）的**项目级主指令源**。工具专属文件（如 `CLAUDE.md`）可镜像本内容，但不得与之冲突。

## 1. 项目身份

Metrica 是一个开源计量经济学框架，为学术研究和数据分析提供高性能、结构化、可扩展的现代工具链。

- 20 个 Julia Core 包，覆盖从 OLS 到贝叶斯的全部主流模型族
- CLI-first 桌面工作台（Rust + Tauri + React）
- GPL v3 许可
- AI 协作构建（本文件为 AI 助手的项目指令中枢）

Core / Runtime / App 三层架构边界不得混淆。

## 2. 当前阶段：回顾完善

前期建设（S1–S5 全部模型族）已基本完成。当前处于**完善阶段**。

工作优先级：

1. 测试覆盖（补充缺失的测试、golden-value 对齐）
2. Bug 修复（数值精度、边界条件、错误处理）
3. 文档完善（README、教程、协议文档准确性）
4. 开源基础设施（CI、贡献指南、许可证）

明确禁止：

- 新增模型族或 model_type
- 新增包或子系统
- 大范围架构重构

## 3. 不可违背原则

### 3.1 尊重架构

不得模糊以下边界：

- `packages/`：Julia 包代码与协议定义
- `runtime/`：桥接与执行管理逻辑
- `apps/`：桌面应用代码
- `docs/`、`tutorials/`、`datasets/`：支撑资产

UI 代码不得包含计量逻辑。Core 包代码不得依赖 GUI 细节。Runtime 代码不得成为第二套业务逻辑层。

### 3.2 结构优先于功能数量

优先稳定接口、清晰边界与结构化输出。

### 3.3 结构化结果优于展示文本

不得基于解析打印摘要来构建下游行为。所有结果均须表示为结构化数据：`glance`、`tidy`、`augment`、结构化警告、结构化诊断。

### 3.4 教学体验是核心要求

缺失值处理、样本量变化、可读警告均为**一等产品需求**，非可选润色。

### 3.5 禁止静默捏造

不得虚构未定义的 API、不存在的文件、未运行的测试结果、不具备的产品能力。若存在不确定性，须明确写出假设。

### 3.6 语言

核心文档（AGENTS.md、总蓝图、`docs/architecture/`）以简体中文撰写。社区贡献者可使用中文或英文提交 Issue、PR、代码注释。代码标识符、JSON 键名、命令行、仓库路径保持英文。

## 4. 工作顺序

### 阶段 1：诊断

阅读相关代码、测试和文档，确认问题范围和影响面，对照现有协议和架构。

### 阶段 2：最小修复

只改必须改的代码。不做投机式重构。不加新模型或新功能。

### 阶段 3：验证

运行相关测试套件。新增测试覆盖修复的 bug。验证 App 构建和 Runtime 编译。若无法验证，须如实说明。

### 阶段 4：文档同步

更新受影响的文档。若涉及用户可见变更，更新 CHANGELOG。

## 5. 代理式流程规则

适用于长时间运行的代理与多步助手。

### 5.1 实质性工作优先书面计划

若变更触及多个目录、接口或子系统，应先创建书面计划。

### 5.2 假设须可见

未经确认而推进时，将假设记录在设计文档、实施计划或完成总结中。

### 5.3 不隐瞒权衡

若决策影响未来架构，应清楚陈述权衡。

### 5.4 单一职责单元

优先小而聚焦的文件与模块。避免巨型混合职责文件。

## 6. 补全与小范围编辑规则

### 6.1 匹配本地模式

遵循当前目录已建立的命名、布局与风格。

### 6.2 不得夹带架构变更

小范围编辑不得悄悄引入新分层、隐藏依赖或跨模块捷径。

### 6.3 显式优于魔法

选用可读名称，避免不透明辅助链或过度"聪明"的模式。

### 6.4 不留破损占位

不得插入 `TODO`、`TBD`、虚假实现或占位返回值，除非用户明确要求脚手架且知晓代码不完整。

## 7. 仓库特定方向

### 7.1 Core 包的期望

`MetricaBase.jl` 定义共享协议、类型、接口、结构化结果、模型框架语义与警告能力模式。不得沦为估计量实现、UI 逻辑或输出渲染的倾倒场。

### 7.2 Runtime 的期望

`runtime/` 聚焦进程编排、环境管理、请求/响应 schema 处理、日志与取消。不得重复属于 Julia 包的计量逻辑。

### 7.3 App 的期望

`apps/` 聚焦项目工作区流程、数据导入、模型配置、结果展示、导出、教学解释。应消费结构化输出，而非解析终端文本。

## 8. 文档规则

重大架构或工作流变更须更新相关文档。若代码变更使现有文档失效，在同一会话中更新。

### 8.1 控制主文档数量

设计规格（spec）只写架构决策、接口约定、非目标与边界、验收标准。实施计划（plan）只写文件路径、任务目标、关键变更描述、测试要点。禁止在文档中复制完整代码。

### 8.2 临时计划必须回收

短期收口或局部修正的临时文档，在工作并回主文档后应删除。保留的历史文档须标注为已归档。

## 9. 测试与验证规则

完善阶段的核心原则：**测试覆盖不得降低**。每次变更后，受影响的测试套件必须全部通过。新增 bug 修复须附带回归测试。

最低期望：核对文件路径存在、核对协议命名一致性、对变更代码运行针对性测试。对计量代码优先确定性测试、结果形状测试、边界覆盖。

## 10. 变更控制规则

不做无关编辑。不重排大段无关表面。无必要不得重命名或移动文件。不得为简化任务而覆盖用户撰写的内容。

## 11. 完成定义

- 变更符合仓库架构
- 已实现最小预定范围
- 已更新相关文档
- 已执行相关验证，或已说明限制
- 未留下占位逻辑
- 测试覆盖未降低

## 12. 输出风格

汇报已完成工作时：总结实际变更、说明已做验证、说明重要假设或剩余缺口。保持简洁具体。避免夸大信心。

## 13. 指令优先级

1. 用户直接请求
2. 仓库架构与活跃设计文档
3. 本 AGENTS.md
4. 工具适配文件（如 CLAUDE.md）

## 14. 开源社区

Metrica 采用 GPL v3 许可证。

贡献流程：阅读 CONTRIBUTING.md → 从 Issue 列表认领或创建新 Issue → Fork → 分支 → PR → CI 检查 + review 后合并。

行为准则见 CODE_OF_CONDUCT.md。安全漏洞报告见 SECURITY.md。
